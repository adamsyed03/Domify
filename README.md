
  # Domify

  This is a code bundle for Domify. The original project is available at https://www.figma.com/design/WhJ0jkgboJAHawdiP3lpEq/Domify.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  